import React from "react";
import { LoginPencilSketch } from "../../components/auth/LoginPencilSketch";

export const metadata = {
    title: "Sign in - Klyro",
    description: "Sign in to your Klyro developer workspace.",
};

export default function LoginPage() {
    return <LoginPencilSketch />;
}
