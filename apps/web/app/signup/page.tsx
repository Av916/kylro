import React from "react";
import { SignupPencilSketch } from "../../components/auth/SignupPencilSketch";

export const metadata = {
    title: "Create account - Klyro",
    description: "Create your Klyro account and join a workspace built for builders.",
};

export default function SignupPage() {
    return <SignupPencilSketch />;
}
